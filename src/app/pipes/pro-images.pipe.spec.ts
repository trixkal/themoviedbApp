import { ProImagesPipe } from './pro-images.pipe';

describe('ProImagesPipe', () => {
  it('create an instance', () => {
    const pipe = new ProImagesPipe();
    expect(pipe).toBeTruthy();
  });
});
